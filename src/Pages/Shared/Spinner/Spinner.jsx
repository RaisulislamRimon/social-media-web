import React from "react";

const Spinner = () => {
  return (
    <div className="relative">
      <button className="btn loading absolute inset-0">loading</button>
    </div>
  );
};

export default Spinner;
